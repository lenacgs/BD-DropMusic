Como correr o projeto:

No terminal: 

$ mysql -u root -p => corre mysql
source ~/path/to/root.sql => creates DBUser, creates DropMusicDB, uses DBUser
source ~/path/to/DBCreation.sql => creates tables
